# Deployment metadata — safe to keep without secrets (setup-bootstrap.py fills this)
domain: __DOMAIN__
cloudflare_email: __CF_EMAIL__
compose_home: /home/joe/homelab-in-a-box
appdata_root: /opt/appdata/docker-apps
timezone: America/New_York
subdomains:
  auth: auth
  portainer: port
  traefik: traefik
  manager: manager
  rocketchat: sub
