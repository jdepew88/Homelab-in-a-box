# Homelab-in-a-box
#
# Phase 1: python3 scripts/setup-bootstrap.py && ./scripts/compose.sh up -d
# Phase 2: python3 scripts/setup-authelia.py && ./scripts/compose.sh --profile auth up -d

name: homelab-in-a-box

include:
  - path: compose.bootstrap.yaml
  - path: compose.auth.yaml
  - path: compose.rocketchat.yaml
